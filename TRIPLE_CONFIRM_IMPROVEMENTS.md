# 三維共振詳細顯示改進

## 📋 改進內容

升級了「共振 X/3」的顯示方式，讓你能清楚看到各個維度的具體通過情況。

---

## 🔄 改進前後對比

### 改進前
```
共振 2/3                    ← 只知道通過了2個，不知道是哪2個
```

### 改進後
```
共振 2/3 [✓動能,✗籌碼,✓支撐]   ← 清楚看到是「動能」和「支撐」通過，「籌碼」未通過
```

---

## 📊 三維定義（stock_analyzer.html）

### ① 動能層（Momentum）
- **條件**：RSI < 35（超賣狀態）
- **信號**：買方開始收集籌碼
- **顯示**：✓動能 或 ✗動能

### ② 籌碼層（Chip/Volume）  
- **條件**：MACD 買入訊號
- **信號**：技術面轉向上升
- **顯示**：✓籌碼 或 ✗籌碼

### ③ 護城河層（Moat）
- **條件**：護城河評分 ≥ 50（中等以上）
- **信號**：公司競爭力足夠
- **顯示**：✓護城河 或 ✗護城河

---

## 🎯 显示位置

### 1️⃣ 模組三卡片（stock_analyzer.html）
```
模組③ 技術籌碼：85

三維共振 2/3：✓ 動能(RSI<35)  ✗ 籌碼(MACD買入)  ✓ 護城河(≥50)
```

### 2️⃣ 警告訊息（stock_analyzer.html）
```
⚠ 技術框架 2/3 對齊，缺：籌碼(MACD需買入)。建議雙重確認後進場
```

### 3️⃣ 日誌記錄（stock_analyzer.html）
```
共振=2/3[動能✓,籌碼✗,護城河✓]
```

### 4️⃣ 篩選器結果卡片（public/screener.html）
```
共振 2/3 [✓動能,✗籌碼,✓支撐]
```

---

## 💡 使用指南

### 完全對齊（3/3）✅
- 三個維度全部確認
- **建議**：可以立即進場，信號強度最高

### 雙重確認（2/3）⚠️
- 兩個維度確認
- **建議**：應該等待第三個維度確認或使用小額試探
- **缺少情況**：
  - 缺動能：RSI 還不夠超賣，等待更好的進場點
  - 缺籌碼：MACD 還沒買入訊號，等待技術面轉向
  - 缺護城河：公司基本面一般，風險稍高

### 單一信號（1/3）❌
- 只有一個維度確認
- **建議**：等待更多確認，謹慎進場

### 無信號（0/3）🚫
- 沒有維度確認
- **建議**：加入觀察清單，不建議進場

---

## 📝 修改檔案

1. **stock_analyzer.html**
   - 第 821–838 行：重新定義三維共振計算
   - 第 911–916 行：模組三顯示改進
   - 第 951–963 行：警告訊息加入三維詳情
   - 第 976 行：日誌記錄加入三維狀態

2. **public/screener.html**
   - 第 955–975 行：篩選器卡片標籤加入三維詳情

---

## 🔍 技術說明

三維共振的計算邏輯（stock_analyzer.html）：
```javascript
const confirmMom = rsi<35;              // ① 動能：RSI 超賣
const confirmMacd = s.macd==='買入';     // ② 籌碼：MACD 買入
const confirmMoat = moat>=50;            // ③ 護城河：評分中上
const align = [confirmMom, confirmMacd, confirmMoat].filter(Boolean).length;
```

篩選器中使用 VR 量比替代 MACD 籌碼（public/screener.html）：
```javascript
const confirmMom = rsi<35;
const confirmChip = vrChipConfirm||(cVR!==null&&cVR<=0.1);  // VR 量比確認
const confirmSup = vrSup;                                   // VR 支撐線確認
```

---

## ✨ 優勢

1. **透明度高**：清楚看到各維度狀態，不必猜測
2. **風控能力**：知道缺少哪個維度，可以調整策略
3. **學習價值**：理解每個維度的含義，改進交易決策
4. **對齐度判斷**：不只看數字，看具體是什麼對齊了

