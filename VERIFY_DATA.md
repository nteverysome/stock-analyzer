# ✅ 驗證數據補抓狀況

## 🎯 目標

確認補抓的完整歷史數據是否成功，包括：
- 價格記錄數
- 技術指標數
- 股票覆蓋率
- 時間範圍
- 平均記錄數

---

## 方法 1️⃣ : Web 界面驗證（推薦）

### 最簡單的方式

1. **啟動本地開發服務器**
```bash
npx vercel dev
```

2. **在瀏覽器打開驗證頁面**
```
http://localhost:3000/verify.html
```

3. **點擊「🔍 開始驗證」按鈕**

4. **查看驗證結果**

---

## 方法 2️⃣ : Node 腳本驗證

```bash
node scripts/verify-data.mjs
```

**預期輸出**：
```
🔍 驗證數據補抓狀況...

📊 價格記錄:
   總數: 10,000,000 筆
   預期: >10,000,000 筆
   狀態: ✅ 補抓成功

📈 技術指標:
   總數: 10,000,000 筆
   預期: >10,000,000 筆
   狀態: ✅ 計算成功

📍 股票覆蓋:
   總數: 1,962 支
   預期: 1,962 支
   覆蓋率: 100.0%
   狀態: ✅ 覆蓋足夠
```

---

## 方法 3️⃣ : API 驗證

```bash
# 在瀏覽器打開
http://localhost:3000/api/verify

# 或用 curl
curl http://localhost:3000/api/verify | json_pp
```

**返回 JSON 格式的驗證結果**

---

## 🔍 驗證檢查清單

### 價格記錄
- [ ] 總數 >= 10,000,000 筆
- [ ] 預期達成
- [ ] 狀態：✅

### 技術指標
- [ ] 總數 >= 10,000,000 筆
- [ ] 預期達成
- [ ] 狀態：✅

### 股票覆蓋
- [ ] 股票數 >= 1,900 支
- [ ] 覆蓋率 >= 95%
- [ ] 狀態：✅

### 時間範圍
- [ ] 交易日 >= 1,200 天
- [ ] 時間範圍完整
- [ ] 狀態：✅

### 平均記錄
- [ ] 每支平均 >= 3,000 筆
- [ ] 數據分布合理
- [ ] 狀態：✅

---

## 📊 驗證結果解讀

### ✅ 完成狀態

如果看到：
```
✅ 補抓完成！數據準備完整！
   • 共 1,962 支股票
   • 10,000,000+ 筆價格記錄
   • 10,000,000+ 筆技術指標
   可以開始本地測試：npx vercel dev
```

**下一步**：進行本地測試和部署

---

### ⏳ 進行中狀態

如果看到：
```
⏳ 補抓中或部分完成
   請稍候，讓補抓和計算繼續進行
   當前進度：25.5%
```

**說明**：補抓和計算還在進行，請耐心等待

---

### ❌ 未完成狀態

如果看到：
```
❌ 數據補抓未完成
   請檢查補抓腳本是否正在執行
```

**檢查項目**：
1. 補抓腳本是否還在運行？
   ```bash
   tasklist | find "node"
   ```

2. 日誌文件中有沒有錯誤？
   ```bash
   tail -f backfill.log
   tail -f indicators.log
   ```

3. 數據庫連接是否正常？

---

## 🧪 詳細驗證指標

### 價格記錄

```sql
SELECT COUNT(*) FROM tw_daily_prices;
-- 預期: 10,000,000+
```

### 技術指標

```sql
SELECT COUNT(*) FROM tw_indicators;
-- 預期: 10,000,000+
```

### 股票覆蓋

```sql
SELECT COUNT(DISTINCT symbol) FROM tw_daily_prices;
-- 預期: 1,962
```

### 時間範圍

```sql
SELECT 
  MIN(price_date),
  MAX(price_date),
  COUNT(DISTINCT price_date)
FROM tw_daily_prices;
-- 預期: 2000-01-01 至 今日, 交易日 > 1,200
```

### 每支股票平均記錄

```sql
SELECT 
  AVG(cnt),
  MIN(cnt),
  MAX(cnt)
FROM (
  SELECT symbol, COUNT(*) as cnt 
  FROM tw_daily_prices 
  GROUP BY symbol
) t;
-- 預期平均: 5,000-7,000
```

---

## ✨ 驗證成功後

1. **本地測試**
   ```bash
   npx vercel dev
   # 訪問 http://localhost:3000/chart.html
   ```

2. **測試 K 線圖**
   - 輸入 `2330` 查詢
   - 驗證 K 線圖正常顯示
   - 檢查技術指標加載

3. **測試回測功能**
   - 點擊「回測評分」
   - 驗證統計數據顯示

4. **部署到生產**
   ```bash
   git push origin main
   ```

---

## 📞 常見問題

**Q: 驗證失敗怎麼辦？**
A: 
1. 檢查補抓腳本是否還在運行
2. 查看 backfill.log 和 indicators.log
3. 確認數據庫連接正常

**Q: 進度卡住不動了？**
A: 補抓可能因為網路問題停止了，重新執行腳本會自動跳過已有數據

**Q: 驗證頁面打不開？**
A: 確認 `npx vercel dev` 已啟動，且訪問的是 `http://localhost:3000/verify.html`

---

**驗證成功就可以開始測試了！** ✅
